\section{Homology test}\label{sec:homology}

\paragraph{Metric.} For a single chain we score each epitope one-vs-many. Only
equal-length CDR3s can share a Hamming distance, so pairs are counted within length
bins. For epitope $e$ ($N_e$ TCRs) against the other $N-N_e$ TCRs of its cohort,
\[
P_{\text{self}}=\binom{N_e}{2},\qquad P_{\text{non}}=N_e\,(N-N_e)
\]
(equal-length pairs); with $m_{\le d}$/$l_{\le d}$ the within-/against-rest pairs at
Hamming $\le d$ and an exposure-proportional pseudocount $a$ (so S/N${\to}1$ under no
enrichment),
\[
\mathrm{S/N}_e(d)=\frac{(m_{\le d}+a)/P_{\text{self}}}
{\bigl(l_{\le d}+a\,P_{\text{non}}/P_{\text{self}}\bigr)/P_{\text{non}}}.
\]
The dataset value is the geometric mean of $\mathrm{S/N}_e$ over epitopes ($\pm$95\%
CI); the error bars are thus the epitope-to-epitope spread. $d{=}0$ isolates
publicity (identical CDR3s); $d{=}1$ is the substitution-neighbour convergence signal
exploited by curated specificity databases~\cite{Shugay_2017}; signal $\Rightarrow$
S/N $\gg 1$, noise $\Rightarrow$ S/N $\approx 1$.

\paragraph{Result.} Figure~\ref{fig:hom} orders the cohorts:
VDJdb-HQ $>$ TCRvdb-true $>$ TCRvdb-false $>$ VDJdb-LQ $\gg$ immrep25 $>$ OLGA.
On TCR$\beta$ the verified cohorts reach S/N $=\hqHomB$ (VDJdb-HQ) and $\trueHomB$
(TCRvdb-true), whereas immrep25 positives give only $\immHomB$ and \emph{every} control
sits at the floor---including the structured \textbf{AIRR non-random} control
($\airrTHomB$), whose 20 epitopes are the top-50 expanded clonotypes of 20 real donor
repertoires. Even this realistic construction does not reproduce immrep25's within-epitope
homology: each donor's top clones are private expansions (no $\beta$ clonotype is shared
between donors), so the ``epitopes'' carry no convergent structure. immrep25's excess is
therefore specific to its epitope$\to$TCR assignments (i.e.\ publicity,
\S\ref{sec:publicity}), not to abundance or expansion in real data
(AIRR random $\airrHomB$, OLGA random $\olgaRHomB$). The same holds on TCR$\alpha$
(immrep25 $\immHomA$). immrep25's CI excludes $1$ but lies one to three orders of
magnitude below every quality cohort. As the substitution radius grows
(Fig.~\ref{fig:dlines}) the verified cohorts stay high while immrep25 and all controls
remain near the floor across $d=1,2,3$.

\begin{figure}[h]\centering
\input{analysis/fig_homology.tex}
\caption{Homology S/N ($d{=}1$) per cohort, TCR$\alpha$ and TCR$\beta$ (one-vs-many:
each epitope with $\ge 30$ records vs.\ the rest; bar = geometric mean over epitopes,
whiskers = 95\% CI = between-epitope spread). Dashed line: noise floor S/N${=}1$.}
\label{fig:hom}
\end{figure}

\begin{figure}[h]\centering
\input{analysis/fig_dlines.tex}
\caption{Homology S/N vs.\ substitution radius $d=1,2,3$ (one-vs-many geometric mean).
Verified cohorts stay high; immrep25 and all controls track the floor.}
\label{fig:dlines}
\end{figure}

\paragraph{By-epitope breakdown.} Figure~\ref{fig:bee} plots the within-epitope
neighbour rate for every epitope with $n\ge 30$ (one point per epitope; boxplots show
the per-cohort quartiles); the vertical gap to each cohort's non-self rate (red bar)
is the S/N. immrep25's points hug its non-self line, whereas the verified cohorts sit
orders of magnitude above. Table~\ref{tab:epi} lists all 20 immrep25 epitopes: in
total they contribute only $\immWithinB$ (TCR$\beta$) / $\immWithinA$ (TCR$\alpha$)
within-epitope Hamming-$1$ neighbours --- versus $\olgaWithinB$ / $\olgaWithinA$ for
the OLGA random control and tens of thousands for the VDJdb cohorts
--- and even that handful is concentrated in a few epitopes (e.g.\ YLFNADIWI) and is
entirely public (\S\ref{sec:publicity}). By contrast the AIRR non-random donors
contribute essentially no within-epitope neighbours ($\airrTwithinB$ TCR$\beta$), because
each donor's expanded clones are private rather than epitope-convergent.

\begin{figure}[h]\centering
\input{analysis/fig_beeswarm.tex}
\caption{Per-epitope within-epitope neighbour rate (TCR$\beta$, Hamming$\le 1$; each
point is one epitope, $n\ge 30$; boxplots = per-cohort quartiles). Red bars = cohort
pooled cross-epitope (non-self) rate; the vertical gap is the homology S/N.}
\label{fig:bee}
\end{figure}

\begin{table}[h]\centering\small
\input{analysis/immrep_epitope_table.tex}
\caption{immrep25 positives by epitope ($n$ TCRs) and their count of within-epitope
Hamming-$1$ CDR3 neighbours per chain and $\beta$ S/N, versus the structured/random
controls. Sorted by $\beta$ S/N.}
\label{tab:epi}
\end{table}

\paragraph{Graph view.} The S/N ratio is the within- vs.\ between-epitope
edge-density of the homology graph (nodes${=}$CDR3, edges${=}$Hamming${\le}1$; all
epitopes with $\ge 30$ records). Figure~\ref{fig:homgraph} renders it (graphviz
\texttt{sfdp} layout): the verified cohorts form dense epitope-coloured cliques, while
immrep25 and all controls are near-edgeless point clouds with no epitope-coloured
clustering.

\begin{figure}[h]\centering
\includegraphics[width=\linewidth]{../results/figures/fig_homology_graph_TRB.pdf}
\caption{TCR$\beta$ homology graph per cohort (all epitopes $\ge 30$ records). Verified
cohorts form dense epitope-coloured cliques; immrep25 and all controls are near-edgeless.}
\label{fig:homgraph}
\end{figure}
